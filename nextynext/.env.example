# MaskFit AR API configuration. SERVER SIDE ONLY — never expose the secret to the browser.
# Copy to .env.local and fill in the credential issued to your institution.
MASKFIT_AR_API_URL=https://portal.maskfitar.com
MASKFIT_AR_API_KEY=your_api_key
MASKFIT_AR_API_SECRET=your_api_secret
